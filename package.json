{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "ysf:version": "lerna version --conventional-commits",
    "ysf:publish": "lerna publish --conventional-commits"
  }
}
